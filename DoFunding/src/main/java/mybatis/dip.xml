<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dip.model.Dip">
	<insert id="InsertDip">
		insert into dipstate(d_num,m_no,p_num,d_check)
		values(dipseq.nextval,#{m_no},#{p_num},1)
	</insert>

	<select id="SelectDip" resultType="int">
		select d_check from dipstate
		where m_no = #{m_no} and p_num = #{p_num}
	</select>

	<select id="CheckDip" resultType="myDip">
		select * from dipstate
		where m_no = #{m_no} and p_num = #{p_num}
	</select>

	<update id="UpdateLikeDip">
		update dipstate
		set d_check = 1
		where p_num = #{p_num} and m_no = #{m_no}
	</update>

	<update id="UpdateDisLikeDip">
		update dipstate
		set d_check = 0
		where p_num = #{p_num} and m_no = #{m_no}
	</update>

	<delete id="deleteDip">
		delete from dipstate m_no=#{m_no}
	</delete>

</mapper>

