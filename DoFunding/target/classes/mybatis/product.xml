<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="product.model.Product">
	<select id="GetTotalCount" resultType="int">
		select count(*) from do_product
		<if test="whatColumn == 'p_subject'">
			where p_subject like #{keyword}
		</if>
		<if test="whatColumn == 'p_content'">
			where p_content like #{keyword}
		</if>
		<if test="whatColumn=='p_category_fk'">
			where p_category_fk like #{keyword}
		</if>		
		order by p_num desc
	</select>
	
	<select id="ProductList" resultType="MyProduct">
		select * from do_product
		
		<if test="whatColumn=='p_subject'">
			where p_subject like #{keyword}
		</if>
		<if test="whatColumn=='p_content'">
			where p_content like #{keyword}
		</if>
		<if test="whatColumn=='p_category_fk'">
			where p_category_fk like #{keyword}
		</if>
		order by p_num desc
	</select>
	
	<update id="ReadcountUp">
		update do_product set readcount = readcount+1 where p_p_num=#{p_p_num}
	</update>
	
	<select id="GetProduct" resultType="MyProduct">
 		select * from do_product where p_num=#{p_num}	
 	</select>
	
	<insert id="InsertProduct">
 		insert into do_product(p_num,name,company,image,stock,price,category,contents,point,inputdate)
 		values(seqprod.nextval,#{name},#{company},#{image},#{stock},#{price},#{category,jdbcType=VARCHAR},#{contents},#{point},#{inputdate})
 	</insert>
	
	<update id="UpdateProduct">
		update do_product set stock=stock-#{stock} where p_num=#{p_num}
	</update>
	
</mapper>

<!-- product.xml -->


