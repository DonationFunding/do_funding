<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="like.model.like">
<insert id="InsertLike">insert into like(l_num,m_no,p_num,l_check)values(likseq.nextval,#{m_no},#{p_num},1) </insert>

<select id="SelectLike" resultType="int">select d_check from like where m_no = #{m_no} and p_num = #{p_num} </select>

<select id="CheckLike" resultType="dip.model.DipBean">select * from like where m_no = #{m_no} and p_num = #{p_num} </select>

<update id="UpdateLike">update like l_check = 1 where p_num = #{p_num} and m_no = #{m_no} </update>

<update id="UpdateDisLike">update like l_check = 0 where p_num = #{p_num} and m_no = #{m_no} </update>

<delete id="deleeteLike">delete from like m_no=#{m_no} </delete>
</mapper>